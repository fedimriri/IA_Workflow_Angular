version: '3.8'
services:
  frontend:
    build: .
    image: tek-up-frontend:latest
    ports:
      - "4200:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  # Example placeholder for an API service. Uncomment and adjust if you have a backend image.
  # api:
  #   image: your-backend-image:latest
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - NODE_ENV=production
